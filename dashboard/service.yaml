apiVersion: v1
kind: Service
metadata:
  name: dashboard
spec:
  type: NodePort
  selector:
    name: kubernetes-dashboard
  ports:
  - targetPort: 8443
    nodePort: 39000
    port: 8443
    protocol: TCP